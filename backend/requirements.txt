flask==3.1.2
flask-cors==6.0.1
langchain==0.3.27
langchain-openai==0.3.34
langgraph==0.6.8
numpy==2.2.6
pydantic==2.11.10
requests==2.32.5
python-dotenv==1.0.1

# ===== Production Memory System Dependencies =====
# Redis (Intra-session memory)
redis==5.0.1

# MongoDB (Inter-session + Preferences)
pymongo==4.10.1

# Qdrant (Vector search)
qdrant-client==1.7.0

# Optional: For async embedding workers
# celery==5.3.4  # Uncomment if using Celery instead of Redis Streams
# redis[hiredis]==5.0.1  # High-performance Redis parser